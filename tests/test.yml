---
- hosts: localhost
  remote_user: root

  vars:
    sudoers_mailto: test@example.com
    set_sudoers_mailto: true
    sudoers_mail_always: true
    set_sudoers_mail_always: true

  roles:
    - ansible-control-sudoers
